import { Button } from '@/components/ui/button'
import { formatScheduleTime } from '@/features/schedules/schedule-model'
import { overlapsKstHour,type GpuReservation } from './gpu-model'

export function GpuHourlyGrid({day,rows,busy,manage,onCancel}:{day:string;rows:GpuReservation[];busy:boolean;manage:boolean;onCancel:(item:GpuReservation,gpu:number,hour:number)=>void}){
  return <div className="gpu-table-scroll"><table className="gpu-hour-grid"><thead><tr><th scope="col">시간 (KST)</th><th scope="col">GPU_#0</th><th scope="col">GPU_#1</th></tr></thead><tbody>{Array.from({length:24},(_,hour)=><tr key={hour}><th scope="row">{String(hour).padStart(2,'0')}:00</th>{[0,1].map(gpu=>{const matches=rows.filter(row=>row.gpu_ids.includes(gpu)&&overlapsKstHour(row,day,hour));return <td key={gpu}>{matches.length?matches.map(row=><article className="gpu-slot-reservation" key={row.id}><div className="section-heading"><strong>{row.team_name}</strong>{row.can_cancel&&<Button className="button-secondary" disabled={busy} aria-label={`${day} ${String(hour).padStart(2,'0')}:00 GPU_#${gpu} 예약 취소`} onClick={()=>onCancel(row,gpu,hour)}>이 시간 취소</Button>}</div><p>{row.program_name}</p><p>{formatScheduleTime(row.starts_at)} ~ {formatScheduleTime(row.ends_at)} (KST)</p><p className="proposal-text">{row.purpose}</p>{manage&&row.requester_name&&<p className="field-help">신청: {row.requester_name}</p>}</article>):<span className="gpu-slot-free">예약 가능</span>}</td>})}</tr>)}</tbody></table></div>
}
